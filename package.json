{
  "name": "vowel-stack",
  "version": "0.1.0",
  "private": true,
  "description": "Vowel Self-Hosted Stack - Docker Compose workflow for running the full vowel stack locally",
  "workspaces": [
    "client",
    "core",
    "demos/*",
    "engine/packages/*"
  ],
  "scripts": {
    "build": "./scripts/build.sh",
    "build:skip-client": "./scripts/build.sh --skip-client",
    "build:skip-ui": "./scripts/build.sh --skip-ui",
    "stack:up": "docker compose -f docker-compose.cpu.yml up -d",
    "stack:up:gpu": "docker compose -f docker-compose.gpu.yml up -d",
    "stack:up:full": "docker compose up -d",
    "stack:down": "docker compose -f docker-compose.cpu.yml down -v",
    "stack:down:gpu": "docker compose -f docker-compose.gpu.yml down -v",
    "stack:down:full": "docker compose down -v",
    "stack:logs": "docker compose -f docker-compose.cpu.yml logs -f",
    "stack:logs:gpu": "docker compose -f docker-compose.gpu.yml logs -f",
    "stack:logs:full": "docker compose logs -f",
    "stack:test": "bun run scripts/stack-test.ts",
    "stack:sync-secrets": "bun run scripts/sync-secrets.ts",
    "stack:build": "./scripts/build.sh && docker compose build",
    "stack:build:gpu": "./scripts/build.sh && docker compose -f docker-compose.gpu.yml build",
    "stack:build:docker-only": "docker compose build",
    "demo:dev": "cd demos/demo && bun run dev",
    "demo:build": "cd demos/demo && bun run build",
    "client:build": "cd client && bun run build",
    "client:dev": "cd client && bun run dev",
    "core:dev": "cd core && bun run dev",
    "install:all": "bun install && cd client && bun install && cd ../core && bun install && cd ../demos/demo && bun install"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
